{
  "private": true,
  "devDependencies": {
    "async": "^0.9.0",
    "commonform-cli": "^0.4.0",
    "glob": "^5.0.6",
    "js-yaml": "^3.3.1",
    "split2": "^0.2.1",
    "through2": "^0.6.5"
  },
  "scripts": {
    "coverage": "node tests/coverage.js agreement.commonform",
    "lint": "commonform lint < agreement.commonform",
    "scenarios": "node tests/scenarios.js",
    "test": "npm run lint && npm run coverage && npm run scenarios"
  }
}
